version: "3.9"
services:
  frontend:
    image: ishitagallery-frontend
    container_name: ishitagallery-frontend
    build: .
    ports:
      - "127.0.0.1:5000:5000"
    env_file:
      - .env
    restart: always
